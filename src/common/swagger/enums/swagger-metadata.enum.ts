/**
 * The enum that contains Swagger module's metadata keys.
 */
export enum SwaggerMetadata {
  /**
   * The metadata that contains a controller's info.
   */
  API_CONTROLLER = 'swagger-module:api-controller',
}
