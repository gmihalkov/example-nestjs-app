/**
 * The entity that describes an API controller documentation parameters.
 */
export interface ApiControllerOptions {
  /**
   * The human-readable controller's name.
   */
  title: string;

  /**
   * The controller's description.
   */
  description: string;
}
