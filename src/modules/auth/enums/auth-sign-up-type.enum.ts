/**
 * The enum that contains options how user can sign up.
 */
export enum AuthSignUpType {
  /**
   * Directly by a password.
   */
  BY_PASSWORD = 'by_password',

  /**
   * By an invite sent by the existing application member.
   */
  BY_INVITE = 'by_invite',
}
